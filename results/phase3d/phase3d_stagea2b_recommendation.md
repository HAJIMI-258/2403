# Phase 3D Stage A.2b Recommendation

Forced anchor consume reaches the consumer, and the baseline target lineage already has a non-empty recovery surface.

Do not enter Stage B. The next repair is frame-local assignment routing: at frame 990 the target proposal is being consumed by an unrelated `active_match` before the matched-lineage recovery path can run.

Only move forward after the target re-entry proposal can reach the lineage-2 recovery consumer without being preempted by lineage-0 active-match traffic.
